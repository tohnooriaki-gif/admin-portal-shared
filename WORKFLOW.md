# 作業ルール（共通）

admin-portal 統合ポータル配下の全セッション（admin-portal / admin-portal-shared / price-app / receipt-app）に
共通する作業ルール。リポジトリ固有のルール（触ってよい範囲・そのアプリ独自の検証手順など）は
各リポジトリの `NewSessionStart.md` / `CLAUDE.md` 側に書く。このファイルは admin-portal-shared の
mainブランチを都度読みに行く前提（配布・タグ管理はしない。詳細は下記「変更のしかた」参照）。

## 検証

- コードを変更したら `npx tsc --noEmit` で型チェックする
- テストコードがあるリポジトリ（現状 price-app）は `npm test` も実行する
- 可能なら実機（ブラウザ）で動作確認する

## コミット・プッシュ

- コミット・プッシュはユーザーから明示的に指示があったときだけ行う
- 「回答だけ」と言われたら実装はせず、案の提示に留める
- **例外: receipt-app**は、動作確認（型チェック・ローカルでの画面確認）まで済んでいれば、
  都度の確認なしでコミット・プッシュしてよい（ユーザーが2026-09-15に明示許可）。ただし
  データを消す・書き換える等の元に戻せない操作や、認証まわり等の影響範囲が大きい変更は、
  この例外の対象外（従来通り一言確認してから進める）

## 他リポジトリとの関わり方

- 統合ポータルは4つの独立したリポジトリ・セッションで構成される
  （admin-portal / admin-portal-shared / price-app / receipt-app）
- 自分の担当リポジトリ以外のコードは勝手に読み書きしない。参照が必要なら
  ユーザーに確認するか、該当セッションにメッセージで問い合わせる
- `admin-portal-shared` の変更が必要な場合は、変更内容と影響範囲をユーザーに伝えた上で
  対応する（バージョン管理の詳細は `admin-portal-shared/README.md` 参照）
- **例外: このファイル(`WORKFLOW.md`)自体**は、上記の「他リポジトリを勝手に編集しない」ルールの
  対象外。admin-portal / price-app / receipt-app のどのセッションからでも直接編集・コミット・
  push してよい（詳細は下記「変更のしかた」）

## 報告

- 作業完了後の報告には、変更したフォルダパスと（該当すれば）動作確認したURLを含める
- 各リポジトリ直下に `CHANGELOG.md` を持ち、開発作業（コード変更・ドキュメント変更等。
  アプリ内のユーザー向け操作履歴とは別物）を日付ごとに記録する。形式は既存の
  `receipt-app/CHANGELOG.md` を踏襲（凡例: 🏗️ インフラ／🆕 追加／🔄 変更／🐛 修正／
  🗑️ 削除／📝 その他、日付見出し＋目次）。コミットするたびに逐一書く必要はないが、
  作業のまとまり（1セッション・1トピック単位が目安）ごとに追記する

## 変更のしかた

このファイルは `admin-portal-shared` の他のファイル（`src/` 配下のコード等）とは扱いが違う：

- **`WORKFLOW.md` はどのセッションから編集してもよい**（自分の担当リポジトリ以外を触らない、
  という通常ルールの唯一の例外）。mainにマージすれば即座に「現在の共通ルール」になる。
  exportsを通したコード配布ではないので、タグを切って各アプリ側が明示的に上げる、という
  運用の対象外
- 編集前に最新のmainをpull（他セッションが同時に編集している可能性があるため）
- 追記・修正は自分に関係する範囲に留め、他リポジトリ向けの既存ルールを勝手に削除・変更しない
- コミット・push自体は各リポジトリの通常のコミットルール（上記「コミット・プッシュ」節）に従う
- `admin-portal-shared` の他のファイル（`src/` 配下のコード・`README.md`・`package.json` 等）は
  この例外の対象外。従来通り admin-portal-shared 担当セッションに変更を依頼する
